# --- repo-only docs (reference, never applied to $HOME)
docs/**

# --- chezmoi self config (never track)
.config/chezmoi/**

# --- tmux plugins (3rd party binaries, node_modules, false-positive secrets)
.config/tmux/.tmux/plugins/**

# --- common build / runtime artifacts (prevent future false positives)
**/node_modules/**
**/dist/**
**/.cache/**

# --- nvim auto-generated files
.config/nvim/lazy-lock.json

{{- if ne .chezmoi.username "giho.seong" }}
# --- work-only commands and database helpers
.config/brew/Brewfile
.config/justfile
.config/justfile/**
.config/mysql
.config/mysql/**
.config/zsh/tests
.config/zsh/tests/**
.mysqlsh
.mysqlsh/**
# --- work-only home areas (scaffolded only on the work machine)
10_Database
10_Database/**
20_Work
20_Work/**
{{- end }}

# --- Karabiner legacy config (Kanata only needs the VirtualHIDDevice runtime)
.config/karabiner/**
Library/Preferences/org.pqrs.Karabiner-Elements*.plist
Library/Preferences/org.pqrs.Karabiner-EventViewer.plist
Library/Preferences/org.pqrs.Karabiner-Updater.plist

# --- db-connect: 스크립트는 관리하되, 서버 목록(hosts)은 계정/호스트 정보라 제외
# (다른 PC에서는 ~/.config/mysql/hosts 만 수동으로 넣으면 `db` 동작)
.config/mysql/hosts

# --- justfile: PROD 인프라 레시피는 로컬 전용 (공개 repo에 내부 호스트명 노출 방지)
.config/justfile/prod.just

# --- 자격증명·비밀: 절대 repo(공개)에 들어가면 안 됨 ---------------------------
.soluto_dm.env
.config/redshift
.config/redshift/**
.config/harlequin
.config/harlequin/**
.claude.json
.claude.json.*
.claude/settings.local.json
.config/gh/**
.config/gcloud/**
.config/configstore/**
.config/opencode/**
.config/mycli
.config/mycli/**

# --- 히스토리·로그·OS 노이즈 --------------------------------------------------
.zsh_history
.bash_history
.mysql_history
.duckdb_history
.mycli-history
.viminfo
.mycli.log
.mycli.test.log
**/.DS_Store
