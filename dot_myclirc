# Intentionally (almost) empty.
#
# The real configuration lives in ~/.config/mycli/myclirc, which is managed by
# chezmoi. mycli loads that first and then lets this file override it, so any
# key set here would silently win -- keep it empty.
#
# This file must still exist: mycli rewrites ~/.myclirc from the packaged
# defaults whenever it is missing (see MyCli.__init__), and those defaults would
# then shadow ~/.config/mycli/myclirc.

[main]
